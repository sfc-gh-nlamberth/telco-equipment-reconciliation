[project]
name = "equipment-reconciliation-app"
version = "1.0.0"
description = "Equipment Reconciliation Dashboard"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.50.0",
    "snowflake-snowpark-python>=1.14.0",
    "altair>=5.5.0",
]
